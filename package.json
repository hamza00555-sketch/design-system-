{
  "name": "miswadah-monorepo",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "pnpm -r --filter \"./packages/*\" build",
    "test": "vitest run",
    "typecheck": "pnpm -r exec tsc --noEmit",
    "emulators": "firebase emulators:start"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "firebase-tools": "^15.28.1",
    "playwright": "^1.62.1",
    "tsup": "^8.5.0",
    "typescript": "^5.9.3",
    "vitest": "^3.2.4"
  }
}
